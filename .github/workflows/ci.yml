# Barra push/PR que quebra o front: typecheck, lint, testes e build de produção.
name: ci

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  front:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npx eslint . --quiet
      - name: Testes
        run: npm test
      - name: Build
        run: npm run build
        env:
          # O build só precisa de uma URL válida; a de verdade vem da Vercel.
          VITE_API_URL: https://api.example.invalid/
